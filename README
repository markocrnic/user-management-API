User-management API for authenticating users and returning JWT.

Building docker image:
docker build -t [tag-name] https://github.com/markocrnic/user-management-API.git

Running docker image:
docker run -d -p 5006:5000 [user-management-API-tag-name]
